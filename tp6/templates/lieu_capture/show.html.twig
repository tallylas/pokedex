{% extends 'base.html.twig' %}

{% block title %}LieuCapture{% endblock %}

{% block body %}
    <h1>LieuCapture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu_capture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lieu_capture.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lieu_capture_index') }}">back to list</a>

    <a href="{{ path('lieu_capture_edit', {'id': lieu_capture.id}) }}">edit</a>

    {{ include('lieu_capture/_delete_form.html.twig') }}
{% endblock %}
