{% extends 'base.html.twig' %}

{% block title %}PokemonsCaptures{% endblock %}

{% block body %}
    <h1>PokemonsCaptures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokemons_capture.id }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ pokemons_capture.sexe }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ pokemons_capture.niveau }}</td>
            </tr>
            <tr>
                <th>Xp</th>
                <td>{{ pokemons_capture.xp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pokemons_captures_index') }}">back to list</a>

    <a href="{{ path('pokemons_captures_edit', {'id': pokemons_capture.id}) }}">edit</a>

    {{ include('pokemons_captures/_entrainer_form.html.twig') }}
    {{ include('pokemons_captures/_delete_form.html.twig') }}
{% endblock %}
