{% extends 'base.html.twig' %}

{% block title %}PokemonRef{% endblock %}

{% block body %}
    <h1>Détails de {{ pokemon_ref.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokemon_ref.id }}</td>
            </tr>
            <tr>
                <th>PokemonId</th>
                <td>{{ pokemon_ref.pokemonId }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pokemon_ref.nom }}</td>
            </tr>
            <tr>
                <th>CourbeXP</th>
                <td>{{ pokemon_ref.courbeXP }}</td>
            </tr>
            <tr>
                <th>Evolution</th>
                <td>{{ pokemon_ref.evolution ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pokemon_ref_index') }}">back to list</a>

    <a href="{{ path('pokemon_ref_edit', {'id': pokemon_ref.id}) }}">edit</a>

    {{ include('pokemon_ref/_delete_form.html.twig') }}
{% endblock %}
